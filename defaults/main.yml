zfs:                        true
beefy_version:              true
allow_vbox_repo:            false
allow_contrib:              false
omit_postgres_dir_admin:    false

need_role_ioannis:          true
need_role_postgres:         true
need_role_nagios:           true
need_role_vagrant:          true
need_role_root:             true
need_docker:                true
need_slack:                 true
need_postfix:               true
need_prometheus:            true
need_pgbouncer:             true
need_apache:                true
need_www_pages:             false
need_adobe_flash:           false
need_tastyworks:            false
need_zoom:                  false
pool:                       zz
zpool:                      zz
oracle_java:                true
postgres_passwd:            changeme
thinkorswim:                false

users:
    - { name: ioannis,     passwd: "{{postgres_passwd}}", attr: createdb    }
    - { name: nagios,      passwd: "{{postgres_passwd}}"                    }


